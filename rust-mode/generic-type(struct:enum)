# -*- mode: snippet -*-
# name: generic-type(struct:enum)
# key: 
# --
pub struct Queue<T> {
  older: Vec<T>,
  younger: Vec<T>
}

let mut q = Queue::<char>::new(); // generic struct type-associated function

enum Option<T> {
    None,
    Some(T)
}

enum Result<T, E> {
    Ok(T),
    Err(E)
}
