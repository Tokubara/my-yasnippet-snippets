# -*- mode: snippet -*-
# name: mathematica_src_block_graphics
# key: mg
# --
#+begin_src mathematica :results file graphics :output-dir ../vx_attachments/ :file `(my/vx-pasted-image-name "pdf") (symbol-value 'tmp-img-path)`
p = $0;
Export["../vx_attachments/`(symbol-value 'tmp-img-path)`", p];
#+end_src
