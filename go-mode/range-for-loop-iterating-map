# -*- mode: snippet -*-
# name: range-for-loop-iterating-map
# key: 
# --
for key, value := range map[string]int{"one": 1, "two": 2, "three": 3} {
  // for each pair in the map, print key and value
  fmt.Printf("key=%s, value=%d\n", key, value)
}
