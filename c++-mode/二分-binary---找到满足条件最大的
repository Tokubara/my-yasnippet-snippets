# -*- mode: snippet -*-
# name: 二分-binary---找到满足条件最大的
# key: 
# --
int mid;
while (l < r) {
  mid = (l + r + 1) >> 1;
  if () { // mid 满足条件
    l = mid;
  } else { // mid 不满足条件
    r = mid - 1;
  }
}
