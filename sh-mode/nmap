# -*- mode: snippet -*-
# name: nmap
# key: 
# --
nmap -p3389,20-100 10.130.1.43 # 没有 -p 默认是 1-10000, port scan
nmap -sn 10.211.55.0/24 # 不扫描 tcp 端口, 只看存活主机
nmap -p port -sV ip # 探查 ip 这个 port 的服务详细一点的信息
nmap -T4 -F 192.168.3.1/24,192.168.1.1-192.168.1.100 # 快速扫描网段, 也快速扫描端口(不会扫描所有 1-10000 端口, 所以更快)
nmap -O 172.24.0.32 # 探查主机信息, 慢
nmap -A 172.24.0.7 # 也是探查主机信息, 但是更详细, 很慢
